# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Node modules (dependencies)
# Ignoring node_modules to prevent version-controlled bloat
/node_modules

# Yarn PnP (Plug'n'Play) files
/.pnp
.pnp.*

# Yarn specific folders
# We only want to keep patches, plugins, releases, and versions for Yarn.
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Coverage reports from testing
/coverage

# Next.js build and output files
# Next.js specific folders for production or development builds.
.next/
out/

# Build directory for production
/build

# Miscellaneous files that should not be in the repo
.DS_Store              # macOS file
*.pem                   # Private keys, certificates, etc.

# Debug log files
npm-debug.log*          # npm debug logs
yarn-debug.log*         # Yarn debug logs
yarn-error.log*         # Yarn error logs
.pnpm-debug.log*        # pnpm debug logs

# Environment variable files
# .env files (choose to commit if necessary, though these typically should be ignored for security reasons)
.env*
# This will ignore any .env files like .env.local, .env.production, etc.

# Vercel deployment folder (for projects deployed via Vercel)
.vercel

# TypeScript specific files
*.tsbuildinfo            # TypeScript build information files (intermediate files generated)
next-env.d.ts            # Automatically generated by Next.js for type declarations
